rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /timeline/{doc} {
      allow read: if true;
    }

    match /spotlight/{doc} {
      allow read: if true;
    }

    match /research/{doc} {
      allow read: if true;
    }
  }
}
